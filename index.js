import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent regista o seu componente App como a raiz da aplicação
// e trata de funcionalidades como o fast refresh.
registerRootComponent(App);